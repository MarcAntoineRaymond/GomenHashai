apiVersion: v1
kind: Service
metadata:
  name: {{ include "kintegrity.fullname" . }}-webhook-service
  labels:
    {{- include "kintegrity.labels" . | nindent 4 }}
    {{- with .Values.webhook.service.extraLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.webhook.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.webhook.service.type }}
  selector:
    app.kubernetes.io/name: kintegrity
    {{- include "kintegrity.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.webhook.service.port }}
      protocol: TCP
      targetPort: {{ .Values.webhook.service.targetPort }}
